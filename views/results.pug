doctype html
html(lang='en')
    head
        link(rel='stylesheet' href='/css/style.css')
        title Header

    body
        #Results
            if type == 'fruits'
                each page in results
                    p #{page.title}
                    br
            if type == 'personal'
                p personal